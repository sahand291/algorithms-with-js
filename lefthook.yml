pre-commit: 
  parallel: true
  commands: 
    lint: 
      glob: "*.{js,ts}"
      run: pnpm lint {staged_files}
    format:
      glob: "*.{js,ts}"
      run: pnpm format {staged_files}
commit-msg:
  parallel: true
  commands:
    lint-commit-msg:
      run: pnpm commitlint
